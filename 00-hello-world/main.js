/*
 * The simplest React component ever.
 *
 * It jsut renders an H1 tag into the body of the page. Since
 * JSX is basically just HTML, you can use any valid tag you want.
 */

ReactDOM.render(
  <h1>Hello, World!</h1>,
  document.getElementById('root')
);